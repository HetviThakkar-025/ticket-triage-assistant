# Google AI Studio key used for both embeddings and the decision model
GEMINI_API_KEY=

# Secret used to sign JWTs -- generate with:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=

# --- optional overrides -------------------------------------------------
# GEMINI_MODEL=gemini-3.5-flash
# GEMINI_EMBEDDING_MODEL=models/gemini-embedding-001
# EMBEDDER_BACKEND=auto           # auto | gemini | sentence-transformers | tfidf
# RETRIEVAL_TOP_K=8
# JWT_EXPIRY_MINUTES=60
# TICKET_DB_PATH=tickets.db
# API_BASE_URL=http://127.0.0.1:8000   # used by streamlit_app.py
